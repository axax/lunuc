import defaultConfig from '../gensrc/config.mjs'

export const getDynamicConfig = () => {
    //config
    if(defaultConfig.CONFIG_ABSPATH) {
        // TODO read config file

    }

    return defaultConfig
}
